using UnityEngine;

public class Bootstrap : MonoBehaviour
{
    //This is obviously not a bootstrapper but it sounds cool so shut the fuck up

    void Awake()
    {
        Application.targetFrameRate = 60;
    }
}
